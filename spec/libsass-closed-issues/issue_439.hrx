<===> input.scss
@mixin odd( $selector, $n) {
  $selector: "& + " + $selector + " + " + $selector;
  $placeholder: unique_id();
  %#{$placeholder} { @content; }
  #{$selector}:first-child {
    #{$selector} { @extend %#{$placeholder}; }
  }
}

ul > {
  @include odd( li, 5 ) { background: #ccc;  }
}

<===> output.css

<===> warning
DEPRECATION WARNING: The selector "ul > + li + li:first-child + li + li" is invalid CSS and can't be an extender.
This will be an error in Dart Sass 2.0.0.

More info: https://sass-lang.com/d/bogus-combinators

  ,
6 |     #{$selector} { @extend %#{$placeholder}; }
  |     ^^^^^^^^^^^^ invalid selector
  |                    ======================== @extend rule
  '
    input.scss 6:5   odd()
    input.scss 11:3  root stylesheet

<===> warning-libsass
DEPRECATION WARNING on line 6, column 5 of input.scss:
The selector "ul > + li + li:first-child + li + li" is invalid CSS.
Therefore, it can't be an extender.
This will be an error in LibSass 5.0.0.

More info: https://sass-lang.com/d/bogus-combinators

  ,
6 |     & + li + li { @extend %#{$placeholder}; }
  |     ^^^^^^^^^^^
  '
    input.scss 6:5   odd()
   ,
11 |   @include odd( li, 5 ) { background: #ccc;  }
   |   ^^^^^^^^^^^^^^^^^^^^^
   '
    input.scss 11:3  root stylesheet
